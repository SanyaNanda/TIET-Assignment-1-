#include<stdio.h>
// determining largest number usin if else statement
main()
{
int a,b,c;
if(a>b);
	{
	printf(" Largest no is %d",a);
	}
else
	if(b>c);
		{
		printf("Largest no is %d",b);
		}
	else
		{
		printf("Largest no is %d",c);
		}
	return 0;
return 0;
}
